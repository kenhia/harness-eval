# Copilot Instructions

Follow the root `AGENTS.md`; GitHub Copilot CLI loads it as a repository
instruction surface. Do not duplicate that contract here because both files
enter the prompt.

Skills live under `.github/skills/`. Path-specific rules live under
`.github/instructions/`.
