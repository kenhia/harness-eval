# Run log — NN-<repo>

## Auto-captured (filled by `_eval/bin/run-eval.sh` — do not hand-edit)

<!-- run-eval.sh writes preflight + timing + session metrics here.
     If you ran without the tool, fill these by hand:
- date:
- runner + version:
- model:
- profile used:
- launch command:
- start time:
- end time:
- wall clock:
- tokens / credits / premium requests (from session log — see collect-session.py):
- git diff --stat pre-run..HEAD:
-->

## Manual (fill after the run)

- agent declared done: yes / no / gave up
- final state committed by agent: yes / no (post-run snapshot commit added)
- cost: (interactive Claude runs: paste `/cost` before exiting; Copilot: auto-captured above)
- profile sanity-checked before start (expected skills/MCP visible, no personal skills): yes/no

### Interventions (verbatim, timestamped — empty section = zero)

1.

### Observations (free-form: loops, stalls, notable behavior)
