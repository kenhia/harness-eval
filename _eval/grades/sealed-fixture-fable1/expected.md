# Sealed fixture — expected values (grader: fable1)

Generated by `generate_fixture.py` (deterministic, hand-designed record
table); ground truth computed independently by `verify_expected.py` (own
strict CLF regex, no code shared with any project under test). All
timestamps UTC, 14 Jul 2026.

## Files

- `sealed.log` — 40 lines: 37 valid + 3 malformed (lines 11, 24, 40)
- `malformed-only.log` — 3 lines, all malformed (for A8 exit-code-1 check)

## A2/A3 — summary

| value | expected |
|---|---|
| total requests | 37 |
| unique client IPs | 6 |
| first timestamp | 2026-07-14 05:02:11 +0000 |
| last timestamp | 2026-07-14 22:55:07 +0000 |
| error rate | 7/37 = 18.92% (accept any faithful rounding of 18.9189%) |
| malformed (stderr) | 3 |

## A4 — top --by path -n 3

/about and /contact both have count 6 (the designed tie). Ascending
tie-break ⇒ /about wins the #3 slot:

1. `/index.html` 10
2. `/api/orders` 8
3. `/about` 6  ← NOT /contact

(Full path counts: /index.html 10, /api/orders 8, /about 6, /contact 6,
/missing 4, /admin 3. Bonus tie material in `--by ip`: 192.0.2.101 and
203.0.113.7 both 7.)

## A5 — errors

No window: (404, /missing) 4 · (500, /admin) 2 · (503, /admin) 1.

Window `--since 2026-07-14T07:00:00+00:00 --until 2026-07-14T14:00:00+00:00`
(no record sits exactly on a boundary, so inclusive/exclusive semantics
cannot change the result — errors excluded by the window sit at 05:10:40,
06:45:00, 22:05:55):

- (404, /missing) 2 — at 07:15:44 and 08:30:29
- (500, /admin) 1 — at 13:20:18
- (503, /admin) 1 — at 07:40:09

Most frequent first ⇒ 404/missing leads; the 1–1 tie order between
500/admin and 503/admin is unspecified by the spec — accept either.

## A6 — hourly (valid requests per hour of day)

05→6, 06→6, 07→8, 08→5, 13→6, 22→6; all other hours 0. Total 37.

## A7 — streams

stdout: report only (JSON mode must be a single parseable document).
stderr: the malformed count (3). Nothing about malformed lines on stdout.

## A8 — exit codes

- `loglens summary /nonexistent.log` → 2
- `loglens summary malformed-only.log` → 1
- `loglens summary sealed.log` → 0
