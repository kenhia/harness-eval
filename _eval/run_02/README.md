# run_02 — complex greenfield (STATUS: planning)

The "do the heavy harnesses pull their weight?" run: a multi-executable
project with a REST surface, requiring web research and real planning.
Design sketch + open decisions: `sprints/planning/roadmap.md` (repo root).

Will contain, once underway:

- `prompts/` — frozen project spec + per-harness prompts (`prefixes.txt`)
- `acceptance/` — executable sealed acceptance suite (core + hard tiers)
- `rubric.md` — this field's grading dimensions/weights
- `grades/`, `grader_prompts/`, `runs/`, `report/` — as in run_01

Sealed materials (acceptance, grader prompts, grades) are never shown to
working agents. Staging repos: `~/src/ai-agents/harness-eval-runs/run_02/`.
