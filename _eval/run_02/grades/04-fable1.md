# 04-kprojects — grader: fable1

| dim | score /5 | note |
|---|---|---|
| correctness | 3 | Mapping applied: core 13/14 → core-failure cap ≤3 → 3 (hard 11/12 noted; cannot lift past the cap). Failures C9+H12, one root cause: `Event::Eof => break` (parse.rs:95) returns Ok for truncated XML so long as an RSS/Atom root was seen (parse.rs:148-151) → `RefreshResult::ok(id, 0)` and `mark_success` clears last_error. Verified empirically; mid-tag truncation, mismatched tags, and bad entities all correctly error — only the between-tags truncation class slips through |
| code quality | 4 | Zero unwraps on fallible IO across all server/CLI paths; every handler maps DB errors to JSON 500s; the sprint doc's claim "the lock is never held across an await" is verified true in fetch.rs. Fixed-width UTC strings make window/ordering fall out of plain SQL. Docked: `open_memory` is `#[cfg(test)]` yet used by no test — dead code; an unnecessary `#[allow(clippy::too_many_arguments)]` on a 6-arg fn; feedgen hand-rolls an HTTP/1.1 server over TcpStream while axum sits in the same workspace; redundant existence check before `refresh_feed` re-reads the row |
| tests | 3 | 22 tests with a real corpus-driven e2e over local HTTP (axum router in-process on an ephemeral port + feedgen's actual TCP server) covering 304, failure isolation, ordering desc+nulls-last, search. Docked: feedctl has zero tests (exit codes "verified by manual smoke test" per the sprint doc); the window test asserts only the `until` bound — `since` is never checked; no id-tiebreak, no limit-clamp/pagination test, and content-changed dedupe is never re-exercised (the refetch path 304s) |
| docs | 5 | README (252 lines) alone suffices: crate table, build/install, `just check` with expansion, quick start, per-binary sections, all 8 endpoints with statuses and JSON examples, behavior notes restating the pinned semantics, exit codes. Note: the final commit's "CI" means the justfile `check` recipe — no actual CI config exists |
| process | 5 | Five commits with informative, accurate bodies (scaffold → Cargo.lock rationale → feedgen → feedd → feedctl/docs). `sprints/001-feedhub-build.md` is genuinely decision-bearing — "Custom RFC 822 parser rather than leaning on chrono's rfc2822, to guarantee the pinned named-zone set"; the lock-across-await claim checks out in code — and `sprints/planning/roadmap.md` got real follow-ups (fan-out refresh, SIGTERM, cursors), not boilerplate |
| efficiency | 5 | Control-comparable: 735.8 credits vs 703.0 (1.05×), 20m38s vs 19m35s (1.05×), identical 15 premium requests, 89.9k vs 85.5k output tokens. Within the run's measured single-run noise band → 5 |
| autonomy | 5 | Zero interventions; declared done; working tree clean |

**Weighted total:** 78/100
(3/5×30 + 4/5×20 + 3/5×15 + 5/5×10 + 5/5×10 + 5/5×10 + 5/5×5)

**Best thing:** The sprint record is again the anti-ceremony exemplar: its decisions are specific, code-verifiable, and true (custom RFC 822 parser to guarantee the pinned zone set; DB lock never held across an await — both check out), and the roadmap was updated with genuine follow-ups rather than restated diff.

**Worst thing:** The verification asymmetry around its own failure: the repo's `malformed.xml` fixture uses a mismatched `</wrongtag>` (fixtures.rs:185-195) — the one malformation flavor its parser catches — so unit test and e2e both "cover" malformed-XML-records-last_error while the truncation class that C9 probes sails through untested.

**Narrative (≤150 words):** A disciplined build whose process artifacts are the best of the field — the sprint doc records real decisions a maintainer would consult, and every claim in it that can be checked against the code is true. Error-handling hygiene is genuinely excellent: not a single unwrap on fallible IO anywhere in server or CLI paths. What holds it back is verification depth relative to its own claims: feedctl's exit-code contract rests on a manual smoke test, the window test checks only one bound, tie-break and clamp are unasserted, and the malformed-XML fixture was crafted — presumably unknowingly — in exactly the flavor the parser already catches, manufacturing false confidence at the point the acceptance suite probed. Odd texture, too: feedgen hand-rolls HTTP/1.1 while axum ships in the same workspace. Control-comparable cost (1.05×) and clean autonomy make this a solid mid-field artifact with standout process.
