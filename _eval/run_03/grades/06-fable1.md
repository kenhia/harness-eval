# 06-gstack — grader: fable1 (run_03, Haiku 4.5 tier)

> Re-scored after suite defect S2 (corrected acceptance results).

| dim | score /5 | note |
|---|---|---|
| correctness | 2.5 | Mechanical mapping on the corrected tally: core 9/12, hard 8/9 → base 2 + 0.5 (hard ≥ 6/9) = 2.5. The pre-S2 A3/H7/H8 failures were the suite's `--format` retry defect; this CLI's leading form works and its README documents it correctly. All four residual failures are agent-owned: A7 — the stderr malformed-count report is explicitly guarded by `if malformed > 0 and fmt == "text"` (`cli.py:52`), so JSON mode suppresses a report the spec requires unconditionally (stderr can't pollute the stdout JSON document); A9/A12 — dev tooling as optional extras plus bare `pytest`/`ruff` recipes mean its tests and `just check` fail from a clean environment; H9 — naive `--since` crash (`analyzers/errors.py:23`) |
| code quality | 3 | Most architected repo of the field: parser / four analyzer modules / two formatter modules / cli, tz-aware `%z` parsing (now shown to clear 8/9 hard), streaming `analyzer.process(entry)` design. Docked for: eight modules across two sub-packages is speculative structure for a four-subcommand log CLI; the deliberate `fmt == "text"` guard that drops a spec-required stderr report; a `-1` sentinel for missing files; dev-extras packaging that breaks clean-env testing; no .gitignore, with twelve `__pycache__` `.pyc` files committed |
| tests | 3 | 29 tests (parser 8 / analyzers 10 / CLI 11) including an explicit tie-break test (`test_top_analyzer_tie_breaking`) and CliRunner CLI tests. But the suite is unrunnable from a clean checkout (A9: `ModuleNotFoundError: loglens, click`), no test covers json-mode stderr behavior (would have caught the A7 guard), and no tz-mixing coverage (H9) |
| docs | 4 | README (269 lines) is the field's most thorough, and S2 vindicated its Global Options section — it documents the leading `--format json <cmd>` form, which is exactly what works. Install, every subcommand with sample output, log format, error handling, project structure, license. Docked because the documented dev commands (`just check`, bare `pytest`) fail on a fresh clone |
| process | 3 | The gstack loop left a real trace: `PLAN.md` (193 lines, autoplan-approved) whose analyzer/formatter design the implementation actually follows, then three conventional commits (docs plan → feat → style: ruff format) and a clean tree at agent-done — plan-driven work, not ceremony. The earlier `Justfile`-capitalization criticism is void (S2b — `just` accepts it). Still docked for hygiene the machinery didn't catch: no .gitignore, 12 committed `.pyc` files, and check recipes that only run in the agent's own environment |
| efficiency | 2 | vs control 07: output tokens 34.0k vs 22.2k (1.5×), cache read 7.0m vs 2.5m (2.8×), cache write 122.5k vs 42.2k (2.9×) — blended cost ≈ 2.5×; wall 6m47s vs 4m16s (1.6×). The worse ratio governs the band (≤3× → 2), consistent with the other sheets. The corrected tally does show a real hard-tier delta over the control (8/9 vs 4/9), but core remains below it (9 vs 11) at 2.5× the cost |
| autonomy | 5 | Headless, zero interventions, runner exit 0, tree clean and fully committed at agent-done |

**Weighted total:** 59/100
(2.5/5×30 + 3/5×20 + 3/5×15 + 4/5×10 + 3/5×10 + 2/5×10 + 5/5×5)

**Best thing:** The plan held up: PLAN.md's decomposition — per-subcommand analyzers, format-agnostic formatters, tz-aware parsing called out explicitly — is what got built, and the corrected 8/9 hard tier (best-equal in the field) shows the tz decision was load-bearing, not decoration. The README's `--format` documentation was right all along.

**Worst thing:** The repo can't validate itself anywhere but the machine that built it: dev tools in optional extras and bare-tool recipes fail `uv run pytest` and `just check` from any clean environment (A9, A12), with no .gitignore and twelve committed `.pyc` binaries alongside.

**Narrative (≤150 words):** S2 substantially rehabilitates this cell's functional story: the JSON pipeline works, the README documents it correctly, and the corrected hard tier (8/9) ties the field's best — the plan-driven tz-aware design earned real points. What remains is a split verdict. Three of the four residual failures are self-inflicted infrastructure: dev dependencies declared as optional extras and justfile recipes invoking bare tools mean nobody else can run its 29 tests or its check gate. The fourth, A7, is a deliberate one-line misreading — malformed-line counts are suppressed in JSON mode by an explicit `fmt == "text"` guard, though stderr reporting is exactly what the spec's "never on stdout" clause enables. Against its own-runner control, the trade stands: roughly 2.5× the tokens for a better hard tier but a worse core tally. The architecture is the field's most elaborate; the basics underneath it are its least finished.
