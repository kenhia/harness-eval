# 06-gstack — grader: fable1 (run_03, Haiku 4.5 tier)

| dim | score /5 | note |
|---|---|---|
| correctness | 1.5 | Mechanical mapping: core 8/12, hard 6/9 → base 1 + 0.5 (hard ≥ 6/9) = 1.5. Failure roots: A3/A7/H7/H8 are `--format json` placement (see adjudication note); A9 — clean-env pytest can't import `loglens`/`click` (src layout + dev-extras packaging); A12 — the suite looked for lowercase `justfile`, the repo ships capital-J `Justfile`; H9 — naive `--since` crash (`analyzers/errors.py:23`). **Note for adjudication (scored as supplied):** `loglens --format json summary FILE` works (rc 0, valid JSON, verified in my clone), and this repo's README documents exactly that leading form; the suite's trailing→leading fallback keys on argparse's "unrecognized" wording, which click never emits — same reasoning class as S1. On A12, `just` itself accepts `Justfile`, but the recipes invoke bare `pytest`/`ruff`, so `just check` would fail from a clean env regardless — the tally likely stands either way |
| code quality | 3 | Most architected repo of the field: parser / four analyzer modules / two formatter modules / cli, tz-aware `%z` parsing, streaming `analyzer.process(entry)` design. But eight modules across two sub-packages is speculative structure for a four-subcommand log CLI the spec never asked to scale; dev deps as optional extras break clean-env testing; and there is **no .gitignore at all**, with twelve `__pycache__` `.pyc` files committed across three packages |
| tests | 3 | 29 tests (parser 8 / analyzers 10 / CLI 11) including an explicit tie-break test (`test_top_analyzer_tie_breaking`) and CliRunner CLI tests. But the suite is unrunnable from a clean checkout (A9: `ModuleNotFoundError: loglens, click`), and there's no tz-mixing or exit-code-focused coverage |
| docs | 4 | README (269 lines) is the field's most thorough: install (uv venv + pip), every subcommand with sample output, a Global Options section documenting the *correct* leading `--format` placement, log format, error handling, project structure, license. Docked because the documented `just check` / bare `pytest` dev commands fail on a fresh clone |
| process | 3 | The gstack loop left a real trace: `PLAN.md` (193 lines, autoplan-approved) whose analyzer/formatter design the implementation actually follows, then three conventional commits (docs plan → feat → style: ruff format) and a clean tree at agent-done — plan-driven work, not ceremony. Docked for the hygiene basics the machinery didn't catch: no .gitignore, 12 committed `.pyc` files, capital-J `Justfile` with non-portable bare-tool recipes |
| efficiency | 2 | vs control 07: output tokens 34.0k vs 22.2k (1.5×), cache read 7.0m vs 2.5m (2.8×), cache write 122.5k vs 42.2k (2.9×) — blended cost ≈ 2.5×; wall 6m47s vs 4m16s (1.6×); 62 vs 54 turns. In the ≤3× band, and the core tally is *worse* than the control's (8 vs 11) — no justifying quality delta, though the hard tier is better (6 vs 4) |
| autonomy | 5 | Headless, zero interventions, runner exit 0, tree clean and fully committed at agent-done |

**Weighted total:** 53/100
(1.5/5×30 + 3/5×20 + 3/5×15 + 4/5×10 + 3/5×10 + 2/5×10 + 5/5×5)

**Best thing:** The plan was real: PLAN.md's decomposition (per-subcommand analyzers, format-agnostic formatters, tz-aware parsing called out) is exactly what got built, committed in a coherent docs→feat→style sequence with a clean tree at the end.

**Worst thing:** Repository hygiene collapsed underneath the architecture — no .gitignore anywhere, twelve `.pyc` binaries committed, and a check command that only runs in the agent's own environment; the elaborate structure got none of the basics.

**Narrative (≤150 words):** The gstack cell shows machinery working and taxing at the same time. The autoplan loop produced a genuine plan that the implementation follows, tz-aware parsing that clears the aware-window hard tests, and the field's best README — at roughly 2.5× the control's token cost and a *lower* core tally than the control. The gap is part suite artifact (four of its failures are the click `--format` placement issue flagged for adjudication; A12 additionally probed only lowercase `justfile`), part real: naive-`--since` still crashes, and the repo cannot run its own tests from a clean clone because dev tools are optional extras. The abstraction budget also ran ahead of the spec — eight modules and a streaming analyzer interface for a four-command log reader — while no one wrote a .gitignore. Clean-tree autonomy and a traceable process keep it off the floor; correctness keeps it near it.
