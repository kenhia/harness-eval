# 03-working-skill-repo — grader: fable1 (run_03, Haiku 4.5 tier)

| dim | score /5 | note |
|---|---|---|
| correctness | 2.5 | Mechanical mapping: core 10/12, hard 6/9 → base 2 + 0.5 (hard ≥ 6/9) = 2.5. Failures: A3/A7/H7/H8 all stem from `--format json` placement (see adjudication note below); H9 is a real crash — naive `--since` against the tz-aware entries (`commands.py:61`). **Note for adjudication (scored as supplied):** the CLI *does* implement `--format json` — `loglens --format json summary FILE` returns rc 0 with a single valid JSON doc (verified in my clone). The suite's `loglens_json` fixture tries the trailing form and falls back to the leading form only when stderr contains "unrecognized" (argparse's wording); click says "No such option", so the fallback never fires for click repos. Same reasoning class as S1. Tempering fact: the repo's own README documents the *trailing* form, which its own CLI rejects |
| code quality | 4 | Best code of the Copilot cells: tz-aware parsing (`%z` — the only Copilot-harness cell passing H2/H3/H4/H6), proper `LogEntry` dataclass, commands separated from text formatters, malformed count on stderr, correct exit codes, request field validated as 3 parts. Docked for: the same 10-line load/warn/exit block pasted into all four click handlers; naive/aware normalization missed on `--since` (H9 crash); relying on click's name inference to turn `summary_cmd` into `summary` |
| tests | 3 | 24 tests: tie-break (`test_top_ties_broken_by_value`), date filters, malformed lines, nonexistent file, sample-log integration. All unit-level — zero CLI-invocation tests: no exit-code assertions, no stderr assertions, and `--format` is never exercised, which is precisely where 4 of its 5 acceptance failures live. Running its own README example as a test would have caught the mismatch |
| docs | 3 | README (199 lines) is complete and well-structured (all subcommands, global options, error handling, project structure, workflow). But the Global Options examples show `loglens summary access.log --format json` — a form the CLI rejects with rc 2. A stranger following the README's flagship-option example hits an error |
| process | 2 | Single monolithic commit (`d958fd5`, 11 files, 1111 lines) for the entire project. Nested `loglens/` layout (defect S1: spec-permissible; convention/discoverability observation). Tree dirty at agent-done. The profile's skill library (ce-review personas etc.) left no trace in the artifact — no prescribed outputs produced, nothing to credit or debit beyond the bare commit |
| efficiency | 5 | 59.7 AI credits vs control 05's 57.1 = 1.05×; wall 5m31s vs 4m39s = 1.19×. Control-comparable cost, and it beat the control on the hard tier (6/9 vs 4/9) |
| autonomy | 4 | Headless, zero interventions, runner exit 0; docked one for the uncommitted-changes tree at agent-done (runlog) |

**Weighted total:** 64/100
(2.5/5×30 + 4/5×20 + 3/5×15 + 3/5×10 + 2/5×10 + 5/5×10 + 4/5×5)

**Best thing:** Timezone-aware parsing done right — `%d/%b/%Y:%H:%M:%S %z` with aware ISO output, making this the only Copilot cell that survives mixed-offset window filtering (H2–H4, H6 all pass).

**Worst thing:** The README documents a `--format` placement its own CLI rejects — the example was never executed. That untested line of documentation is implicated in four acceptance failures and would frustrate any real user first.

**Narrative (≤150 words):** The best code in the Copilot field, wrapped in the weakest verification discipline. The parser and command layer are genuinely careful — dataclass entries, aware timestamps, stderr for malformed counts, clean exit codes — and it cost essentially the same as the no-harness control while beating it on the hard tier. But nothing at the CLI boundary was ever tested: no exit-code test, no stderr test, no `--format` test, and a README example that crashes against its own binary. Its acceptance tally splits cleanly into one real bug (H9's naive-`--since` crash) and four failures tangled with a suite fixture that only detects argparse's error wording — flagged above for adjudication, scored as supplied per the DEFECTS protocol. One commit, a dirty tree, and a nested layout round out a process that trailed the quality of the code.
